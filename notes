Python - docker exec -it <container name> /bin/bash
        cd app/
        python3 <py file name>


Node - localhost:3000


PHP - localhost:8000
        Fill credentials


Adminer <DB Demonstration> - localhost:8080 
                                Server - db
                                Username - root
                                Password - example
                                Database - user


Mongo - docker exec -it mongodb mongo
        show databases
        use <random name>
        db.createCollection("random name")


Redis - docker exec -it redis redis-cli
        SELECT 1 <selects db n1>
        SET mykey "Hey" <creates a key named "Hey">
        GET mykey <shows your key>
        INFO keyspace <shows how many keys you have in each db>
        KEYS * <shows your keys>


php - mysql
nodejs - mongodb
python - redis



SET NAMES utf8;
SET time_zone = '+00:00';
SET foreign_key_checks = 0;
SET sql_mode = 'NO_AUTO_VALUE_ON_ZERO';

DROP TABLE IF EXISTS `user`;
CREATE TABLE `user` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `first_name` varchar(50) NOT NULL,
  `last_name` varchar(50) DEFAULT NULL,
  `
